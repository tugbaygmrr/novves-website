/** Site navbar (fixed) altında içerik — ürün kataloğu ile aynı */
export const LEGAL_CENTER_PAGE_PADDING_TOP = "pt-20 sm:pt-24 lg:pt-28" as const;

/** Mobil çekmece: site navbar altından */
export const LEGAL_CENTER_MOBILE_DRAWER =
  "top-20 h-[calc(100dvh-5rem)] sm:top-24 sm:h-[calc(100dvh-6rem)]" as const;
